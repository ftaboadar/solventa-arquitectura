# Material de referencia del curso (transcripciones, PDFs de clase) — uso local como guía,
# no se versiona por ser contenido de terceros (posible material con derechos del curso/universidad).
utils/

# Código de los experimentos de arquitectura (semanas 6-7)
node_modules/
dist/
*.log
.env

# Terraform (infra/ de cada experimento) — .terraform.lock.hcl SÍ se versiona (pin de providers)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars

